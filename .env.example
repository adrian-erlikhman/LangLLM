# Copy to .env (never commit .env). One key for every subject model + the Llama prompt writer.
OPENROUTER_API_KEY=sk-or-v1-...
# Optional: shown on the OpenRouter dashboard as the app name
OPENROUTER_APP_TITLE=LangLLM
